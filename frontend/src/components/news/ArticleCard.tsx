import Image from 'next/image';
import Link from 'next/link';
import { formatDate } from '../../lib/utils';
export type ArticleCardProps = { article: { id: string; title: string; slug: string; excerpt?: string | null; thumbnail?: string | null; publishedAt?: string | Date | null; category?: { name: string; slug: string } | null; }; };
export function ArticleCard({ article }: ArticleCardProps) {
  return <article className="overflow-hidden rounded-3xl border border-white/10 bg-white/5 backdrop-blur-sm transition hover:-translate-y-0.5 hover:border-amber-300/30"><Link href={`/news/${article.slug}`} className="block"><div className="relative aspect-[16/10] overflow-hidden bg-slate-900"><Image src={article.thumbnail || 'https://dummyimage.com/1200x675/0f172a/f8fafc&text=BCN'} alt={article.title} fill className="object-cover" sizes="(max-width:768px) 100vw, 33vw" /></div><div className="space-y-3 p-5">{article.category ? <p className="text-xs uppercase tracking-[0.2em] text-amber-300">{article.category.name}</p> : null}<h3 className="line-clamp-2 text-xl font-semibold text-white">{article.title}</h3>{article.excerpt ? <p className="line-clamp-3 text-sm leading-6 text-slate-300">{article.excerpt}</p> : null}{article.publishedAt ? <p className="text-xs text-slate-400">{formatDate(article.publishedAt)}</p> : null}</div></Link></article>;
}